<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">
    <!--value的值填写你在友盟后台申请的应用Appkey  5e828f8d167edd2e0a000052-->

    <!--友盟统计所需权限-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--登录-->
        <activity android:name=".ui.activity.login.SplashActivity">
            login: Activity
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->
            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".ui.activity.login.LoginActivity" />
        <!--底部弹窗-->
        <activity
            android:name=".ui.activity.ExchangeActivity"
            android:launchMode="singleTask"
            android:theme="@style/TopDialogStyleBottom" />
        <!--微信-->
        <activity
            android:name=".widgets.wxapi.WXEntryActivity"
            android:exported="true" />
        <activity android:name=".ui.activity.ProductDetailsActivity" />
        <!--提交订单-->
        <activity android:name=".ui.activity.Submit0rdersActivity" />
        <!--（有）选中收货地址-->
        <activity android:name=".ui.activity.SelectAddressActivity" />
        <!--添加收货地址-->
        <activity android:name=".ui.activity.AddressMessage" />
        <!--订单详情-->
        <activity android:name=".ui.activity.IineItemActivity" />
        <!--完成订单-->
        <activity android:name=".ui.activity.CompleteOrderActivity" />
    </application>

    <!--友盟统计配置Appkey与Channel-->
    <!--value的值填写你在友盟后台申请的应用Appkey-->
    <!--value的值填写渠道名称，例如yingyongbao。这里设置动态渠道名称变量-->
    <!--<meta-data android:value="${UMENG_CHANNEL_VALUE}" android:name="UMENG_CHANNEL"/>-->
</manifest>